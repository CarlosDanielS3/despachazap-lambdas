{
  "name": "@despachazap/shared",
  "version": "1.0.0",
  "scripts": {
    "build": "rm -rf dist && tsc && cp package.json dist/nodejs/ && cd dist/nodejs && npm install --production --legacy-peer-deps"
  },
  "dependencies": {
    "mongoose": "^8.9.3",
    "@aws-sdk/client-s3": "^3.738.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.2"
  }
}
